{
  "extends": "solhint:recommended",
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": "error",
    "avoid-throw": "error",
    "avoid-suicide": "error",
    "avoid-sha3": "error",
    "reentrancy": "error"
  }
}
